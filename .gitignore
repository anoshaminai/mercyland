# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local state (settings + worktree checkouts of this repo)
.claude/

# Pre-compression photo originals parked by scripts/optimize-photos.sh. Kept out of git
# so the full-size camera files don't land in history if you forget to delete them.
*.originals/
